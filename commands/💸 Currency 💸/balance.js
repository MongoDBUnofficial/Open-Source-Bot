const { MessageEmbed } = require("discord.js");

module.exports = {
    name: "balance",
    category: "💸 Currency 💸",
    description: "Your crypto coins balance.",
    usage: "`c!balance`",
    run: async ( client,message, args) => {
    
        message.reply("In Construction")

    }

}